for (let i = 0; i < 7; i++) {
    let row = ""; // Initialize an empty string for each row
    for (let j = 0; j < i + 1; j++) {
      row += "#"; // Append a "#" to the row string
    }
    console.log(row); // Log the complete row after constructing it
  }
  